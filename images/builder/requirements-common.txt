# Common dependencies shared across JoustMania services
#
# These are pre-installed in the builder image to speed up service builds.
# Update this file when adding new dependencies used by multiple services.

# gRPC - all services use gRPC for inter-service communication
grpcio>=1.70.0
grpcio-health-checking>=1.70.0

# OpenTelemetry - observability across all services
opentelemetry-distro>=0.49b0
opentelemetry-exporter-otlp>=1.28.0
opentelemetry-instrumentation-grpc>=0.49b0

# Metrics - Prometheus metrics in all services
prometheus-client>=0.19.0
psutil>=5.9.0

# Common utilities
pyyaml>=6.0

# Audio processing deps (numpy/numba/resampy) were removed in #911: the audio
# service now uses soxr (wheels, no compilation) and installs numpy+soxr from
# its own pyproject. Pre-installing them here leaked ~300 MB of numba into
# EVERY service runtime image via the site-packages COPY in service Dockerfiles.

# Build tools needed for some dependencies
setuptools>=45
wheel
