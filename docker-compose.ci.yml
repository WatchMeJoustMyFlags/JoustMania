# CI Override - No hardware dependencies
#
# Use this for CI/testing environments without sound hardware:
#   docker-compose -f docker-compose.yml -f docker-compose.ci.yml up
#
# Or with the override file for port exposures:
#   docker-compose -f docker-compose.yml -f docker-compose.override.yml -f docker-compose.ci.yml up
#
# Note: We must fully redefine services to clear device/volume lists
# (docker-compose merges arrays, doesn't replace them)

services:
  # Audio Service - No sound device required (play_audio=off via CI flagd override)
  # Fully redefined to remove devices list
  audio:
    # Clear devices list from base compose (requires docker compose v2.24.0+)
    devices: !reset []
    volumes:
      - ./services/audio/assets:/app/services/audio/assets:ro
    environment: !override
      OTEL_SERVICE_NAME: audio-service
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      FLAGD_HOST: flagd
      FLAGD_PORT: "8015"

  # flagd - Use the complete CI flag directory (play_audio=off, backend=mock).
  # Single source of truth (#959): instead of overlaying per-file <domain>.ci.json
  # onto the base path — which made flagd serve different content at the same
  # in-container path, invisibly (the #957 trap) — mount the whole
  # services/flagd/ci directory in place of base services/flagd. flagd's
  # --uri file:/etc/flagd/<domain>.json list is unchanged; the directory CONTENT
  # differs. The integration harness sets FLAGD_FLAG_DIR to this same host
  # directory, so the file a test writes IS the file flagd serves. !override
  # REPLACES the base [./services/flagd:/etc/flagd] mount (compose MERGES array
  # values by default; !override replaces the whole list).
  flagd:
    volumes: !override
      - ./services/flagd/ci:/etc/flagd

  # Controller Manager - Mock mode via flagd (backend=mock in ci/controller.json)
  controller-manager:
    # Clear devices and volumes from base compose (requires docker compose v2.24.0+)
    devices: !reset []
    volumes: !reset []
    privileged: false
    # Override depends_on to remove rust-hid and python-hid (hardware profile, not available in CI)
    depends_on: !override
      flagd:
        condition: service_started
    environment: !override
      OTEL_SERVICE_NAME: controller-manager-service
      OTEL_SERVICE_NAMESPACE: infrastructure
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317

  # Game Coordinator - Countdown and rainbow timing now controlled via flagd game domain (Issue #464)
  game-coordinator:
    environment: !override
      OTEL_SERVICE_NAME: game-coordinator-service
      OTEL_EXPORTER_OTLP_ENDPOINT: http://otel-collector:4317
      # Raise the concurrent-games cap for the integration suite only (#779).
      # The coordinator defaults to 1 (production default, unchanged); the
      # concurrent/shadow-isolation tests need multiple live sessions on one
      # stack. This override stays in CI compose; production keeps cap=1.
      GAME_MAX_CONCURRENT_GAMES: "4"

  # Menu - writes game.json/user.json via FlagConfigWriter (game selection, admin
  # settings). Point it at the SAME CI flag directory flagd serves (#959) so a
  # menu-driven flag write lands in the file flagd reads, not a stale base copy.
  # !override replaces the base [./services/flagd:/etc/flagd] mount.
  # Flag-dir permission init (#1031) — point it at the CI flag dir so the nonroot
  # menu can persist admin writes to the same files flagd serves. !override
  # replaces the base [./services/flagd:/etc/flagd] mount with the CI dir.
  flag-dir-init:
    volumes: !override
      - ./services/flagd/ci:/etc/flagd

  menu:
    # The menu persists admin-mode settings (sensitivity, num_teams, game
    # selection) by writing game.json/user.json in the bind-mounted CI flag dir.
    # The image runs as nonroot (uid 65532). #1031: flag-dir-init makes the CI flag
    # dir group-writable, so the menu persists writes as nonroot — the `user: "0"`
    # hack is gone and the prod-like (nonroot) admin write path is exercised in CI.
    volumes: !override
      - ./services/flagd/ci:/etc/flagd
    environment:
      # Shorten the admin trigger-hold force-start window for integration tests
      # so they don't have to sleep the full 3s real-play hold (#823).
      - ADMIN_FORCE_START_SECONDS=0.6

  # Pairing Daemon - Requires hardware, excluded from CI
  pairing-daemon:
    profiles:
      - hardware  # Only starts when 'hardware' profile is active

  # Rust HID Service - Requires hardware, excluded from CI
  rust-hid:
    profiles:
      - hardware  # Only starts when 'hardware' profile is active

  # Python HID Service - Requires hardware, excluded from CI
  python-hid:
    profiles:
      - hardware  # Only starts when 'hardware' profile is active

  # Agent - OTLP fan-out target, not required for integration tests
  agent:
    profiles:
      - agent  # Only starts when 'agent' profile is active

  # Connect-proxy - Not required for integration tests
  connect-proxy:
    profiles:
      - dashboard  # Only starts when 'dashboard' profile is active

  # Dashboard - Not required for integration tests
  dashboard:
    profiles:
      - dashboard  # Only starts when 'dashboard' profile is active

  # Envoy - Not required for integration tests (depends on dashboard)
  envoy:
    profiles:
      - dashboard  # Only starts when 'dashboard' profile is active
