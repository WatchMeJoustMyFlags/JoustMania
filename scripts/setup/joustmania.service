[Unit]
Description=JoustMania Docker Compose Application
Requires=docker.service
After=docker.service network-online.target joustmania-observability.service
Wants=network-online.target joustmania-observability.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/home/pi/JoustMania
ExecStartPre=+/bin/bash -c '\
  docker network inspect joustmania-network >/dev/null 2>&1 || docker network create joustmania-network; \
  docker ps -aq --filter "name=joustmania-" | xargs -r docker rm -f 2>/dev/null; \
  docker container prune -f 2>/dev/null; \
  true'
ExecStart=/home/pi/JoustMania/scripts/setup/joustmania-start.sh
ExecStop=/usr/bin/docker compose down
TimeoutStartSec=600
User=pi
Group=pi

# Restart policy
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
